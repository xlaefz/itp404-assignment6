<div class = "container">
    <div class = "row">
      <div class = "col-md-4">
        {{#each model.artists as |artist|}}
        {{link=to 'artists.artist' artist.id}}
          {{artist.name}}
        {{/link-to}}
        {{/each}}
      </div>
      <div vlass = "col-md-8">
        {{outlet}}
      </div>
    </div>
  </div>
  
